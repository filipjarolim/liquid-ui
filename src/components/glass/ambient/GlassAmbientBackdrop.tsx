'use client';

/**
 * @deprecated No longer used — page content is captured directly by sticky header glass.
 * Kept as a no-op export so existing imports do not break.
 */
export function GlassAmbientBackdrop() {
	return null;
}
