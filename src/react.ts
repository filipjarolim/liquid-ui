/**
 * React bindings for liquidglass-ui.
 * Optional — the core engine works with any framework via custom elements.
 *
 * @example
 *   import { GlassChip, GlassRoot, buildGlassProps } from 'liquidglass-ui/react';
 */
export * from './components/glass';
